#Clic derecho en carptea vacía, abrir con git bash, en la terminal escribir "init + (nombre de la carpeta)"
#Esa carpeta la abrimos con VS Code y le creamos un archivo
#En la terminal escribimos git add (nombre del archivo)
#Si escribimos git commit -m "(el mensaje que queramos para reconocer)" hace un checkpoint
#Si nosotros seguimos escribiendo código sin el commit no se guardará en el git aunque lo guardemos en vscode
#git log --oneline , para tener el historial de cambios, esto varía si estoy en una rama u otra
#git branch -m (nomrbre de la rama)
#git checkout (nombre de la rama)
#git branch -l , de ésta manera vemos las ramas existentes
#git branch -D (nombre de la rama) , para borrar la rama
#git merge (nombre de la rama) , para ya incorporar la rama al código
#si al hacer git log --oneline tomo uno de los commits y despues git checkout (commit copiado) puedo viajar a ese punto
print('Hola Mundo')

print('Adios Mundo Cruel')

